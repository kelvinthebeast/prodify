mixin form-change-multi(path)
  form(action=path, method="POST", class="form-change-multi d-flex align-items-center gap-3 p-3 rounded-4 bg-white shadow-sm w-100 flex-wrap border border-1 border-secondary-subtle")
    .flex-grow-1
      select(name="type", class="form-select form-select-sm modern-select")
        option(disabled selected hidden) — Select Action —
        option(value="active") ✅ Activate Selected
        option(value="inactive") 💤 Deactivate Selected
        option(value="delete-all") 🗑️ Delete All
        option(value="change-position") 🔁 Change Position

    input(type="text", name="ids", value="", class="form-control d-none")

    button(type="submit", class="btn modern-btn") 🚀 Apply
